import { Text, View, StyleSheet, Image } from 'react-native';
import { Button } from 'react-native-paper';

export const ExamenScreen = ({ navigation }) => {
  return (
    <Text>></Text>
  );
};

export default ExamenScreen;
